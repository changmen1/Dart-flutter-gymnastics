/**
 * Dart数据类型 List（数组、集合）
 */
void main() {
  // 1.第一种定义List的方式
  // var l1 = ["张三", 20, true];
  // print(l1);
  // print(l1.length);
  // print(l1[0]);

  // 2.第二种定义List的方式  制定类型
  // var l2 = <String>["张三", "李四"];
  // print(l2);

  // 3.第三种定义List的方式
}
